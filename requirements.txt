# Core data + classical ML
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# Explainability
shap>=0.44
lime>=0.2
matplotlib>=3.7

# LLM step
openai>=1.40
python-dotenv>=1.0
tqdm>=4.66            # progress bar during the 500-post LLM run

# Notebooks (all work happens in these, not bare scripts)
notebook>=7.0
jupyterlab>=4.0
ipykernel>=6.29
